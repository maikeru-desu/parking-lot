const initialState  = {
    events: [],
    event: {},
    loading: false
}

const eventsReducers = (state = initialState, action) => {
    switch(action.type) {
        default:
            return state;
    }
}

export default eventsReducers;